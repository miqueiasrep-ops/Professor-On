rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Server-mediated security: our backend Express app is the sole gatekeeper
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
